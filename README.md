Project for a photobooth type raspberry pi 5 + camera project.
Detects your face, overlays an emoji + frame + text (I personally wasn't a fan of adding the text layer but it was a group project)
Uses the raspberry pi 5 camera module
